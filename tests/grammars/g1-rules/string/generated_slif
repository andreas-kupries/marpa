:start ::= A
inaccessible is warn by default
lexeme default = action => [values]
A ::= @LEX:@STR:<string>
      action => [values]
      assoc => left
@LEX:@STR:<string> ~ 'string'
                     latm => 1
