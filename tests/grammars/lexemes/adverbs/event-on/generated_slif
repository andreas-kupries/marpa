:start ::= B
inaccessible is warn by default
lexeme default = action => [values]
B ::= A
      action => [values]
      assoc => left
:lexeme ~ A
          event => snafu = on
          pause => before
A ~ 'a'
