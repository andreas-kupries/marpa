:start ::= B
inaccessible is warn by default
lexeme default = action => [values]
B ::= A
      action => [values]
      assoc => left
:lexeme ~ A
          event => snafu = off
          pause => before
A ~ 'a'
