
help-recipes: 
Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl debug
	==> debug-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl debug-binary-packages
	==> debug-package-marpa

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl debug-package-marpa
    Install debug-built package marpa 0

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl debug-packages
	==> debug-binary-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl forever
    No operation, infinite loop. Debugging helper (use with -trace).

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl gui
    Graphical interface to the system.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl help
	==> help-options
	==> help-recipes

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl help-dump
    Print the help in Tcl format.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl help-options
    Print the help about options.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl help-recipes
    Print the help.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl install
	==> install-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl install-binary-packages
	==> install-package-marpa

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl install-package-marpa
    Install package marpa 0

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl install-packages
	==> install-binary-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl list
	==> list-options
	==> list-recipes

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl list-options
    List all available options, without details.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl list-recipes
    List all available recipes, without details.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl meta-status
    Status of meta data for Tcl packages and applications.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl null
    No operation. Debugging helper (use with -trace).

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl reinstall
	==> reinstall-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl reinstall-binary-packages
	==> reinstall-package-marpa

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl reinstall-package-marpa
    Reinstall package marpa 0

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl reinstall-packages
	==> reinstall-binary-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl show
	==> show-configuration
	==> show-state

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl show-configuration
    Show the state of the option database.

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl show-state
    Show the state

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl uninstall
	==> uninstall-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl uninstall-binary-packages
	==> uninstall-package-marpa

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl uninstall-package-marpa
    Uninstall package marpa 0

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl uninstall-packages
	==> uninstall-binary-packages

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl wrap4tea
	==> wrap4tea-marpa

Usage: /home/aku/opt/ActiveTcl/bin/kettle -f build.tcl wrap4tea-marpa
    Wrap TEA around package marpa 0


help-options: 
/home/aku/opt/ActiveTcl/bin/kettle --bin-dir <directory>
    Path to binary applications.
    Default is the directory of the tclsh running kettle.
    Default is $(--exec-prefix)/bin should --exec-prefix get
    defined by the user.

/home/aku/opt/ActiveTcl/bin/kettle --collate <min|max|avg>
    Method for coalescing the data from multiple runs (repeats > 0).
    Default is 'min'.

/home/aku/opt/ActiveTcl/bin/kettle --color <boolean>
    Colorize the text written to the terminal during execution.
    Default is platform-dependent.
    * Windows: Off
    * Unix && Terminal:     On
    * Unix && not Terminal: Off

/home/aku/opt/ActiveTcl/bin/kettle --config <path/to/readable/file>
    Path to a file overriding the option configuration in full.
    Used for communication between kettle parent and child
    processes.

/home/aku/opt/ActiveTcl/bin/kettle --constraints <listsimple>
    Tcl list of constraints to activate.

/home/aku/opt/ActiveTcl/bin/kettle --dry <boolean>
    Disable file operations during recipe execution, leaving the
    filesystem untouched.
    Default is 'off'.

/home/aku/opt/ActiveTcl/bin/kettle --exec-prefix <directory>
    Path to the root directory for the installation of binaries.
    Default is $(--prefix).

/home/aku/opt/ActiveTcl/bin/kettle --file <listsimple>
    Tcl list of glob patterns for test files to be run exclusively.
    Default is '*'.

/home/aku/opt/ActiveTcl/bin/kettle --html-dir <directory>
    Path to the root directory to install HTML documentation into.
    Default is $(--prefix)/html.

/home/aku/opt/ActiveTcl/bin/kettle --ignore-glob <listsimple>
    Tcl list of glob patterns for files to ignore in directory scans.
    Default is a list of patterns matching the special directories
    and files of a matter of source code control systems and
    editor backup files.

/home/aku/opt/ActiveTcl/bin/kettle --include-dir <directory>
    Path to the root directory to install C header files into.
    Default is $(--prefix)/include.

/home/aku/opt/ActiveTcl/bin/kettle --iters <1 Inf>
    Number of iterations to perform per benchmark.
    Default is '1000'.

/home/aku/opt/ActiveTcl/bin/kettle --lib-dir <directory>
    Path to binary libraries.
    Default is [info library] of the tclsh running kettle.
    Default is $(--exec-prefix)/lib should --exec-prefix get
    defined by the user.

/home/aku/opt/ActiveTcl/bin/kettle --limitconstraints <boolean>
    Contraint handling. When set run only tests with the active
    constraints (see -constraints).
    Default is '0'.

/home/aku/opt/ActiveTcl/bin/kettle --log <path>
    Log option. Path (stem) for a set of files to log to
    (independent of logging to the terminal).

/home/aku/opt/ActiveTcl/bin/kettle --log-append <boolean>
    Associate to --log. Open files in append mode.
    Default is 'off'.

/home/aku/opt/ActiveTcl/bin/kettle --log-mode <compact|full>
    Verbosity of the logging to the terminal by Tcl-based
    sub-processes like the execution of testsuites and
    benchmarks.
    Default is 'compact'.

/home/aku/opt/ActiveTcl/bin/kettle --machine <boolean>
    Disable human specific output.
    Default is 'off'.

/home/aku/opt/ActiveTcl/bin/kettle --man-dir <directory>
    Path to the root directory to install manpages into.
    Default is $(--prefix)/man.

/home/aku/opt/ActiveTcl/bin/kettle --match <string>
    Run only benchmarks matching the glob pattern.
    Default is the empty string, disabling the filter.

/home/aku/opt/ActiveTcl/bin/kettle --notfile <listsimple>
    Tcl list of glob patterns for test files to be skipped.

/home/aku/opt/ActiveTcl/bin/kettle --prefix <directory>
    Path to the root directory for the installation of any files.
    Default is the twice parent directory of [info library] of the
    tclsh running kettle.

/home/aku/opt/ActiveTcl/bin/kettle --repeats <0 Inf>
    Number of repeats to perform per bench file.
    (Number of runs is 1 + repeats).
    Default is '0'.

/home/aku/opt/ActiveTcl/bin/kettle --rmatch <string>
    Run only tests matching the regexp pattern.
    Default is the empty string, disabling the filter.

/home/aku/opt/ActiveTcl/bin/kettle --single <boolean>
    Run each test case completely independent.
    Default is '0'.

/home/aku/opt/ActiveTcl/bin/kettle --state <path/to/readable/file>
    Path to a file containing shared work state.
    Used for communication between kettle parent and child
    processes.

/home/aku/opt/ActiveTcl/bin/kettle --target <string>
    Critcl build option. Overrides critcl's choice of target
    configuration.

/home/aku/opt/ActiveTcl/bin/kettle --tmatch <listsimple>
    Tcl list of glob patterns.
    Run only the tests matching at least one of the patterns.
    Default is the * (match all), disabling the filter.

/home/aku/opt/ActiveTcl/bin/kettle --tskip <listsimple>
    Tcl list of glob patterns for tests to be skipped.

/home/aku/opt/ActiveTcl/bin/kettle --verbose <boolean>
    Activate tracing of kettle's internal operations.
    Default is 'off'.

/home/aku/opt/ActiveTcl/bin/kettle --with-dia <path/to/readable/file>
    Path to the tool 'dia'.
    Overides kettle's search on the PATH.

/home/aku/opt/ActiveTcl/bin/kettle --with-doc-destination <directory>
    Documentation option. Path to the directory the generated
    documentation is placed in. If a relative path is specified it is
    taken relative to the overall source directory.
    Default is 'embedded'.

/home/aku/opt/ActiveTcl/bin/kettle --with-git <path/to/readable/file>
    Path to the tool 'git'.
    Overides kettle's search on the PATH.

/home/aku/opt/ActiveTcl/bin/kettle --with-shell <path/to/readable/file>
    Path of the shell to run tests, benchmarks, or similar
    Tcl-based sub-processes with.
    Defaults to the tclsh running the kettle build code.
    Default is '/home/aku/opt/ActiveTcl/bin/tclsh'.

